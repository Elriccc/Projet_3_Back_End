# Maintenance

## Images docker
Les images docker sont stockées dans le `.env`.

## Dépendances maven
Les versions des dépendences maven sont tous stockées en temps que variables dans la section `\<properties/>`.

## Dépendances node
Utiliser la commande npm dans le dossier du front-end.  
Le Dockerfile du frontend monte une image avec une version indépendante.